#![allow(unused)]

use rand::Rng;
use std::cmp::Ordering;
use std::fs::File;
use std::io;
use std::io::{BufReader, ErrorKind, Write};

fn main() {
    let is_true = true;
    let my_gradee = 'A'; // Double quotes are for strings, single quotes are for characters
}
